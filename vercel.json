{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "apt-get update && apt-get install -y ffmpeg && npm install"
      }
    }
  ]
}
